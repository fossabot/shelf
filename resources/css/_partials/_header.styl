header
	background: white
	padding: 1em
	display: flex
	-webkit-box-shadow: 0px 2px 4px darken(gray, 10%);
	-moz-box-shadow: 0px 2px 4px darken(gray, 10%);
	box-shadow: 0px 2px 4px darken(gray, 10%);
	.logo
		flex-grow: 1
		&:hover
			text-decoration: none
		h1
			color: teal
			font-size: 1.5rem
			margin: 0
	.search-icon
		i
			color: black
